apiVersion: v1
kind: Secret
metadata:
  name: casdoor-secret
  labels:
    app: casdoor
type: Opaque
stringData:
  # Placeholder — replace with actual PostgreSQL credentials out-of-band.
  # Required keys (see ConfigMap dataSourceName):
  #   postgres-host, postgres-port, postgres-user, postgres-password, postgres-database
  postgres-host: "casdoor-postgresql"
  postgres-port: "5432"
  postgres-user: "casdoor"
  postgres-password: "CHANGE_ME"
  postgres-database: "casdoor"
