apiVersion: v1
kind: Service
metadata:
  name: casdoor
  labels:
    app: casdoor
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: casdoor
